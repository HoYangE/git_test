git 생성
git init -> 최초 1회만(파일 안에 .git 파일생기면 성공)

파일 상태 확인
git status -> 현재 저장소내의 파일들의 상태 확인
	On branch master -> 현재 작업중인 브랜치(master가 기본)
	Untracked files -> 새로 만들어져서 git에 추가해야할 파일들

git에 추가할 파일 알려주기
git add . -> 모든 파일 추가
git add [파일명] -> [파일명] 파일만 추가
pull테스트
